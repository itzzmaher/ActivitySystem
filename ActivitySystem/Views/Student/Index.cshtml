@model IEnumerable<tblRegisterations>
<div class="card-body">
    <div id="v-card" class="row justify-content-center" style="margin-top: 44px;">
        <div class="col-md-9 ">

            <table id="datatablesSimple">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Activity Name</th>
                        <th>Status</th>
                        <th>View</th>

                    </tr>
                </thead>
                <tbody>
                    @{ var i = 1;
                        foreach (var item in Model)
                        {
                            <tr>
                                <td>@i </td>
                                <td>@item.Activity.ActivityName</td>
                                <td>@item.StatusReg.StatusName</td>
                                <td>
                                    @if (item.StatusRegId == 1 || item.StatusRegId == 2)
                                    {
                                        <a asp-controller="Student" asp-action="Cancelation" asp-route-id="@item.GuId" asp-route-status="Cancel" class="btn btn-outline-warning">Cancel </a>
                                    }
                                    <a class="btn btn-outline-info" asp-controller="Student" asp-action="ViewActivtyInfo" asp-route-id=@item.GuId>View</a>
                                </td>
                            </tr>
                            i++;
                        } }
                </tbody>
            </table>
        </div>
    </div>
</div>
<script>
                @{
                    if (@ViewData["Successful"] != null)
                    {
                    <Text>
                    var result = '@Html.Raw(ViewData["Successful"])';
                    Swal.fire(result,'','success')

                    </Text>
                    }

            }
</script>