@model IEnumerable<tblUsers>
<div class="card-body">
    <div id="v-card" class="row justify-content-center" style="margin-top: 44px;">
        <div class="col-md-12 ">
            <table id="datatablesSimple">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>User Name</th>
                        <th>College</th>
                        <th>Role</th>
                        <th>Activation</th>
                        <th>Ststus</th>
                        <th>Modify</th>
                    </tr>
                </thead>
                <tbody>
                    @{ var i = 1;
                        foreach (var item in Model)
                        {

                            <tr>
                                <td>@i </td>
                                <td>@item.Name</td>
                                <td>@item.College.CollegeName</td>
                                <td>@item.Role.RoleName</td>

                                @if (User.IsInRole("Admin") && item.Role.Id == 1)
                                {
                                    <td> </td>
                                    <td> </td>
                                    <td> </td>
                                }
                                else
                                {
                                    <td>

                                        @if (item.IsActive == true)
                                        {
                                            <a asp-controller="Admin" asp-action="DeactivateUser" asp-route-id="@item.GuId" class="btn btn-outline-success">Deactivate </a>
                                        }
                                        else
                                        {
                                            <a asp-controller="Admin" asp-action="AdminActivate" asp-route-id="@item.GuId" class="btn btn-outline-danger">Activate </a>
                                        }

                                    </td>
                                    <td>
                                        @if (item.IsGraduate == false)
                                        {
                                            <a asp-controller="Admin" asp-action="DeleteAccount" asp-route-id="@item.GuId" class="btn btn-outline-success">Delete </a>
                                        }
                                        else
                                        {
                                            <p>Deleted</p>
                                        }
                                    </td>
                                    <td><a asp-controller="Admin" asp-action="ModifyUserInfo" asp-route-id="@item.GuId" class="btn btn-outline-info">Modify</a></td>
                                }
                            </tr>
                            i++;
                        } }
                </tbody>
            </table>
        </div>
    </div>
</div>
<script>
@{
    if (@ViewData["Successful"] != null)
    {
        <Text>
        var result = '@Html.Raw(ViewData["Successful"])';
        Swal.fire(result,'','success')

        </Text>
    }

}
</script>
